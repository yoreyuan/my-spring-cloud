server:
  port: 7070
spring:
  application:
    name: client-a2
eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:cdh6}:${eureka.port:8761}/eureka/
  instance:
    prefer-ip-address: true

